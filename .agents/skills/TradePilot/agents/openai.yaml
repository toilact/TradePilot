interface:
  display_name: "TradePilot"
  short_description: "Repo-specific patterns and workflows for TradePilot"
  default_prompt: "Use the TradePilot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true