# ML training deps — CHỈ cho ml/ (train trên Kaggle). KHÔNG cài vào backend (backend chỉ inference).
# Cài trên Kaggle: pip install -r ml/requirements.txt
# Version pin mềm — Kaggle có sẵn torch/CUDA; ghim pytorch-forecasting để API ổn định.
torch>=2.2
lightning>=2.2
pytorch-forecasting>=1.0
transformers>=4.40  # fine-tune PhoBERT (notebook 02_sentiment_finetune)
pandas>=2.0
scikit-learn>=1.3
# Đọc feature từ backend builder cần các dep backend khi chạy ngoài Kaggle (local smoke-test):
#   sqlalchemy[asyncio], asyncpg, pydantic-settings — đã có trong backend/ env.
